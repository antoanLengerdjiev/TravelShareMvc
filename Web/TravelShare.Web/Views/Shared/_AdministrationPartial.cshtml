@if (this.User.IsInRole("Administrator") || this.User.IsInRole("Moderator"))
{
    <ul class="nav navbar-nav">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administration<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                @if (this.User.IsInRole("Administrator"))
                {
                    <li>@Html.ActionLink("UsersManagement", "index", "userManagement", new { area = "administration" }, null)</li>
                }
                <li>@Html.ActionLink("TripsManagement", "index", "productManagement", new { area = "administration" }, null)</li>
            </ul>
        </li>
    </ul>
}
